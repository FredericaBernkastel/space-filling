I'm diving into whether we can extend beyond finite-dimensional spaces to infinite-dimensional function spaces for the space-filling problem, and whether classical gradient optimization methods can work in that setting. I need to build a rigorous mathematical report with proper LaTeX notation and visualizations, starting with the formal setup of the current problem in compact domains.

Now I'm laying out the different interpretations of "infinite-dimensional" that are mathematically distinct and worth exploring separately: letting the domain itself become infinite-dimensional (like the unit ball in a Hilbert space or L²[0,1]), which is the literal reading of going beyond ℝᴺ. I'm noting the brutal mathematical reality here—concentration of measure and the curse of dimensionality mean that in high-dimensional unit cubes, almost all the volume concentrates near the boundary, which fundamentally changes how optimization and space-filling strategies behave. In infinite dimensions, the unit ball isn't compact, so there's no guarantee the maximum is even attained. The branch-and-bound machinery requires covering Ω with finitely many boxes of small diameter, but infinite-dimensional balls have infinite covering numbers at any scale—the certificate cost becomes unbounded. The test stays sound but loses practical utility.

The real bottleneck isn't the math though—it's the tree structure itself. The branching factor is 2^N children per node, which explodes immediately: at N=10 that's over a thousand children per split, at N=20 it's a million. The current implementation hits a practical wall around N≈8–12 before the mathematical limitations even matter.

There's a straightforward fix: use k-d trees or binary space partitioning to split one axis at a time, keeping the branching factor at 2 regardless of N. This extends the usable range significantly.

Now there's a different direction entirely: instead of placing fixed geometric primitives, I can parameterize shapes themselves as functions—a star-shaped body with a radial profile in L², or a curve in Sobolev space. The design variables now live in infinite-dimensional function spaces, and I'm optimizing over functionals rather than finite parameters. This is shape optimization proper, and the question becomes whether classical gradient methods even apply in function space.

The answer is yes, and it's well-established: Fréchet derivatives, shape derivatives via the Hadamard structure theorem, and Riesz representation to extract a gradient in the Sobolev metric. This is the most promising direction for the space-filling problem—instead of maximizing clearance by moving positions around, I maximize volume covered by varying the shapes themselves, subject to non-intersection constraints. The greedy algorithm (place the biggest ball at the deepest point) becomes just one particular descent direction in this functional landscape.

There's also a third angle: treating the signed distance field itself as the optimization variable.

The state of the ADF is a 1-Lipschitz function vanishing on the boundary, forming a convex subset of the continuous function space. Insertion—replacing the field with the pointwise minimum of itself and a new ball's distance function—is a monotone decreasing operation in this lattice structure, so the algorithm produces a monotone sequence converging pointwise to some limit. This reframes the space-filling problem as characterizing which limits are actually reachable. The greedy approach turns out to be a coordinate-wise or Frank–Wolfe style descent method.

At each step, the algorithm picks the extreme point (deepest location) that maximizes the current linear functional, which is exactly a Chebyshev or greedy approximation algorithm on the space of measures and functions. The clearance decay d_k = ‖g_k‖_∞ relates to Kolmogorov width and covering radius, with the rate d_k ~ c k^{-1/N} in finite dimensions but no decay in infinite dimensions.

Now moving to a kernel-based perspective using RKHS and functional gradient descent.

Neural implicit fields represent the signed distance function as an element of function space approximated by a finite-dimensional manifold, where training becomes Riemannian gradient descent with respect to the metric induced by the neural parameterization. The eikonal regularization ‖∇f‖=1 enforces the Lipschitz/SDF condition as a soft constraint in function space, and Lipschitz-constrained networks using spectral normalization directly encode the Lipschitz regularity requirement.

The key insight is that a neural SDF with a certified Lipschitz constant L can be plugged into the library's proof machinery as a legal `Primitive` with `with_lipschitz(L)`, providing a concrete bridge between neural implicit learning and the formal verification system. Beyond this, the measure-valued formulation treats the distribution of shapes as a probability measure on shape space, where Wasserstein gradient flow (the JKO scheme) enables gradient descent in the infinite-dimensional space of probability measures using the Otto metric rather than Hilbertian geometry.

This connects naturally to centroidal Voronoi tessellations and blue-noise sampling, which are themselves variational in measure space. There's an elegant mathematical thread linking fractal circle packings to Apollonian packings and the Hausdorff dimension of residual sets — the residual set has non-integer dimension, making the space of packings itself fractal-parameterized.

Now addressing whether classical gradient optimization works in function space: in a Hilbert space, the answer is essentially yes — the Fréchet derivative gives a gradient via the Riesz representation, and the iteration proceeds as usual.  The crucial insight is that convergence rates in convex optimization are dimension-free: O(1/k) for smooth convex problems, O(1/√k) for nonsmooth ones. This dimension-independence is precisely why gradient descent remains viable in infinite dimensions.

But in Banach spaces like C(Ω) or Lipschitz spaces, things break down. There's no canonical Riesz isomorphism between the space and its dual, so "gradient" becomes ambiguous — you get a differential functional, but selecting a descent direction requires choosing a metric or preconditioner.

This is where the Sobolev gradient framework comes in: the same functional produces different flows depending on whether you use an L² or H¹ metric, leading to different dynamics like mean curvature flow versus Willmore-type smoothing. The choice of inner product essentially *is* the algorithm design.

Non-differentiability is the real obstacle, and it's the same issue the library already grapples with: the distance field breaks down on the medial axis, and minima of smooth functions lose smoothness. In function space, min operations remain non-smooth, so we need Clarke subdifferentials or the Ekeland variational principle. The library's current approach — using only gradient directions, blending momentum, and monotone acceleration — is already steering toward handling this.

What's elegant is that this strategy generalizes directly to Hilbert space without needing finite dimension, since it never relies on magnitude. The existing line search is actually a Hilbert-space algorithm already; the only finite-dimensional assumption is sampling the gradient via finite differences. In infinite dimensions, that becomes directional derivatives along random finite subspaces — essentially zeroth-order optimization with Gaussian smoothing, where complexity scales with effective dimension rather than ambient dimension.

Global optimization in infinite dimensions is fundamentally impossible without structure — information-based complexity shows the oracle cost is (1/ε)^N, which diverges as N → ∞. But local gradient-based optimization survives precisely because its convergence rate is dimension-free. So the library's two solvers have opposite asymptotics: the exact global solver cannot scale to infinite dimensions, while the gradient-ascent local solver can. That's the core insight — dimension-dependence kills global methods but leaves local methods untouched.

The catch is that Lipschitz certification, which grounds the library's soundness, relies on covering arguments that break down in infinite dimensions. I'm exploring whether we can salvage certification by restricting to compact subsets with controlled covering numbers — like weighted Hilbert cubes where the decay of entropy numbers is polynomial rather than exponential.

The key insight is that this connects to the tractability theory of Sloan and Woźniakowski: if we weight coordinates with summable weights (Σγ_j < ∞), the effective dimension becomes finite and certification stays bounded. This is the rigorous framework from multivariate problem tractability — space-filling in infinitely many dimensions becomes tractable precisely when coordinates are weighted appropriately. So the practical design follows: the adaptive refinement should prioritize axes by weight order, using anisotropic or dimension-adaptive strategies like sparse grids.

The implementation strategy is concrete: replace the full 2^N-tree with a dimension-adaptive binary tree that selects which axis to split based on coordinate importance weights. This gives three immediate benefits — binary splits instead of exponential branching, weight-ordered axis selection, and certification costs that scale with effective dimension rather than ambient dimension. Geometrically, this means shapes in high dimensions should be anisotropic — a ball in ℝ^∞ has zero volume, but cylinders extended along unimportant coordinates work perfectly. The crate already has the right primitives like `ProductBall` and `duocylinder`, which capture exactly this product structure needed for infinite-dimensional spaces.

A second promising direction is space-filling in function space as packing curves or trajectories. Multi-agent motion planning is a natural application: the configuration space becomes a space of paths, clearance is measured as sup-norm distance between trajectories, and the space-filling algorithm generates trajectory tubes where each primitive is a whole path with a radius in path space.

Another angle is packing in image or feature spaces for dataset diversity—instead of packing images in the plane, pack feature vectors in latent space to maximize coverage. This connects to a classic ML problem: greedy farthest-point insertion is the 2-approximation for k-center, and remarkably, this approximation guarantee is dimension-free, which elegantly bridges the space-filling algorithm with determinantal-point-process-like sampling.

The same greedy approach appears in kernel herding and Frank–Wolfe optimization in RKHS, where selecting points that maximize distance in the kernel space gives O(1/k) convergence rates. This is exactly what the statistics literature calls space-filling design—maximin distance designs and Latin hypercubes—so the connection runs deep across multiple fields.

I'm structuring the report to be comprehensive: start with an executive summary, then a table showing what breaks and survives across the two solvers' asymptotics, including the covering-number argument and a practical wall showing how the branching factor explodes with dimension. Then I'll lay out four distinct interpretations of "infinite-dimensional" as a diagram, with a section for each one. Handling non-smooth optimization through Clarke subdifferentials and zeroth-order Gaussian smoothing when gradients aren't available, then leveraging the existing LineSearch as a Hilbert-space algorithm by swapping finite-difference sampling for random subspace exploration. The concrete roadmap prioritizes k-d splits for immediate gains in tractable dimensionality, followed by weighted refinement, anisotropic primitives, and finally a Hilbert-space LineSearch that replaces finite-difference gradients with directional derivatives.

I'm also planning to showcase trajectory packing in path space with sup-norm constraints as the most compelling application, then acknowledge what can't be solved—global optimality guarantees and Lipschitz certificates in infinite dimensions. I'll include a reading list and create a few Mermaid diagrams: a taxonomy of the four approaches, a decision flowchart for solver selection, and possibly a sequence diagram showing the dimension-adaptive splitting algorithm, keeping them simple for GitHub rendering.

For the file location, I'm thinking `doc/infinite_dimensions.md` since it's concise and fits naturally in the repo's documentation structure. I need to double-check the mathematical facts—the boundary concentration formula $(1-(1-2\varepsilon)^N \to 1)$ and the cube's inradius of 1/2 are solid, though the distance to corners grows as $\sqrt{N}/2$, which creates that characteristic "spiky" geometry in high dimensions.

Now I'm verifying the ball volume formula and computing some concrete examples to show how the inscribed ball's volume vanishes super-exponentially—I'll calculate a few values to include in the report rather than just stating the asymptotic behavior.

The covering number bounds for the unit ball follow the standard exponential scaling in dimension. Then there's the Nemirovski–Yudin result on deterministic oracle complexity for Lipschitz global optimization, which shows the problem is fundamentally intractable—requiring roughly $(L/\varepsilon)^N$ function evaluations in the worst case. I'm now moving into gradient descent analysis for smooth convex functions to contrast with this hardness result. Convex sets are weakly compact, so concave upper semicontinuous functionals attain their maximum. But the signed distance function $g(x) = \min_n f_n(x)$ where each $f_n(x) = \|x - p_n\| - r_n$ is convex — a minimum of convex functions is neither convex nor concave, making this a genuinely hard non-concave optimization problem. In finite dimensions this is why branch-and-bound is needed, and in Hilbert spaces weak lower semicontinuity of convex functions still holds but doesn't help here.

For maximization we'd need weak upper semicontinuity, which fails for $g$. Even though $g$ is norm-continuous, it's not weakly upper semicontinuous in general, so the argmax isn't guaranteed to exist on a weakly compact set — though the supremum can still be approached. The key insight is that $f_n$ is weakly lower semicontinuous (since the norm is), so $g$ as a minimum of finitely many weakly lower semicontinuous functions is also weakly lower semicontinuous, but maximizing a weakly lower semicontinuous function on a weakly compact set provides no existence guarantee. I can construct a counterexample in $\ell^2$ using the unit ball with primitives scaled at the standard basis vectors. So for any point on the boundary with $\|x\|=1$, the infimum distance to the centers depends on how far that point is from all the $e_i$, and since the $e_i$ are dense in some sense, I can make $g$ arbitrarily close to $1-r$ but never quite reach it at the boundary. I'm verifying that weighted ellipsoids with diverging coefficients are indeed compact in ℓ², since the diagonal operator with shrinking eigenvalues is compact and the ellipsoid is its image of the unit ball.

The crucial insight is that restoring compactness simultaneously restores both attainment of optima and finite covering numbers, which in turn restores the Lipschitz certificate. The covering numbers scale with the decay rate of the weights — if they grow like $i^{2s}$ (corresponding to Sobolev smoothness), the entropy numbers give $\log N(\delta) \sim \delta^{-1/s}$, which is polynomial rather than exponential in $1/\delta$. This means certification cost becomes polynomial in $1/\epsilon$ with an exponent determined by smoothness, rather than exponential in the dimension.

The elegance here is that infinite dimensions pose no problem as long as the dimensions are ordered by decreasing importance. Now I'm moving toward shape derivatives and Hadamard calculus for shape optimization...

For a functional $J(\Omega)=\int_\Omega j$, the shape derivative depends only on the normal component of the deformation field at the boundary — a key structural result. Then there's the level-set method, which frames shape optimization as gradient flow in function space: you evolve a signed distance function via a Hamilton–Jacobi PDE, and the Lipschitz constraint (keeping $|\nabla\phi|=1$) acts like a reinitialization condition that keeps the representation well-behaved. This connects directly to the library's use of SDFs for shape representation.

Sobolev gradients smooth out the $L^2$ gradient by applying $(I-\Delta)^{-1}$, which is useful for regularization. There's also the Wasserstein/optimal transport perspective: the JKO scheme, Lloyd's algorithm as gradient flow of quantization energy, and blue noise generation all fit into this framework of moving points or measures to minimize some cost.

For k-center problems, farthest-first greedy traversal gives a 2-approximation that works in any metric space and is dimension-free. Kernel herding and Frank–Wolfe methods in RKHS achieve $O(1/k)$ convergence rates. On the neural network side, spectral normalization bounds the Lipschitz constant, and recent work like Liu et al.'s Lipschitz regularization for neural SDFs, IGR/Eikonal, and SIREN all enforce smoothness constraints. I'm also touching on infinite-dimensional quasi-Monte Carlo and weighted Sobolev spaces for tractability analysis.

The Clarke subdifferential handles non-differentiability—the medial axis of a signed distance function is exactly where it's non-smooth, a measure-zero set by Rademacher's theorem. For zeroth-order optimization via Gaussian smoothing, the Nesterov–Spokoiny framework shows that random gradient-free methods scale with ambient dimension, so in infinite dimensions I need to rely on effective dimension rather than true dimension.

The key insight is that derivative-free optimization pays a dimension factor unless the problem has low-dimensional structure like sparsity or an active subspace. When using finite differences along N+1 axes or m random directions in high dimensions, the convergence rate degrades by N/m unless the objective lives on a lower-dimensional manifold. I should present this honestly—gradient descent rates are dimension-free only with the true gradient; derivative-free methods inherently depend on dimension unless the problem has hidden low-dimensional structure. That's a crucial correction to avoid overstating what's possible.

Now I'm thinking about genuinely interesting applications of infinite-dimensional function spaces to the space-filling problem.

One compelling example is packing trajectories in path space—representing collision-free multi-agent motion planning as a space-filling problem in the function space C([0,T], ℝ^d) with the sup-norm metric. A "ball" becomes a tube around a path, and the field measures clearance. Greedy insertion adds each new agent's trajectory as far as possible from existing ones, and the 2-approximation k-center guarantee carries over. The practical implementation uses B-spline coefficients to approximate the function space finitely, and since distance-to-tube is 1-Lipschitz in the sup-norm, the crate's machinery applies directly. This reveals something deeper: the proof is actually metric-agnostic—it works in any metric space where the primitives are 1-Lipschitz distance functions, not just Euclidean space. The only Euclidean-specific part is the box decomposition, so the approach generalizes beyond the original setting.

To extend to general metric spaces, I'd replace the box hierarchy with a cover-tree or navigating-net structure, creating a cover-tree ADF that handles infinite-dimensional and non-vector data like paths, images, or molecules. Beyond that, the same packing framework applies to function-space problems like maximin experimental designs, sensor placement, dataset diversification, and latent-space sampling for generative models. There's also a connection to approximation theory: the sequence of clearances corresponds to the covering radius sequence in Sobolev balls.

The algorithm essentially estimates metric entropy empirically, so I could repurpose GD-ADF as a numerical entropy estimator for function classes. For shape optimization specifically, I'd optimize the primitives' boundaries themselves using shape derivatives, with the crate's signed distance field representation serving as the level-set method's state. Another direction: parameterize each primitive's shape using a Gaussian random field on the sphere via Karhunen–Loève expansion, which gives an infinite-dimensional parameterization with summable weights that naturally restores tractability.

The KL expansion provides exactly the decay structure needed, so random organic shapes become an infinite-dimensional parameter space where importance decreases systematically—this elegantly connects the position optimization and shape optimization approaches. I could also explore infinite-dimensional fractals through Hausdorff dimension and packing exponents, where the decay rate of clearance in the crate's own runs reveals the effective dimension, connecting to Shier's fractal circle packing criteria.

There's a concrete, testable prediction here: plotting the covering radius against the number of points on a log-log scale should show a slope of $-1/N_{\text{eff}}$, which follows from greedy farthest-point insertion in $[0,1]^N$. This gives an empirical way to estimate the effective dimension directly from the algorithm's behavior, and it's something the user could run today with the existing code to validate the theory.

Now I need to put together the deliverable as a markdown report with LaTeX and diagrams.…
---

The central finding is an asymmetry between your two solvers. The Lipschitz certificate is a covering argument, and covering numbers are precisely what explode: $\mathcal N(\Omega,\delta) \ge \delta^{-N}$, infinite when the unit ball isn't compact. Gradient ascent has dimension-free rates in any Hilbert space. So Argmax2D has no infinite-dimensional descendant — that's Nemirovski–Yudin's $\Theta((L/\varepsilon)^N)$ floor, not an implementation deficiency — while GD-ADF conditionally does.

Three results I think are worth your attention:

The obstruction isn't infinite dimension — it's infinite dimension without an ordering of importance. On a weighted ellipsoid ${\sum a_i x_i^2 \le 1}$ with $a_i \to \infty$, the set is the image of the unit ball under a compact operator, hence norm-compact: covering numbers finite, maximum attained, certificate restored. And the cost stops being exponential in $N$ and becomes polynomial in $1/\varepsilon$ with the exponent set by smoothness — at $s=2$, $\log\mathcal N(10^{-2}) \approx 10$. Infinitely many coordinates are affordable provided they matter progressively less, and a Karhunen–Loève expansion supplies exactly those weights for free.

sdf_geq_everywhere stays sound and becomes vacuous — a distinction worth having in writing. The inequality is metric and never mentioned dimension, so it never lies; it simply can't prove anything below the root box. Meanwhile the greedy strategy keeps a guarantee that survives intact: farthest-first traversal is a 2-approximation to $k$-center in any metric space, so insertion quality is dimension-free even where certification isn't. Greedy insertion also turns out to be Frank–Wolfe on the lattice $\mathrm{Lip}_1$, which is where its $O(1/k)$ rate comes from — and your "insertions only ever lower the field" remark is exactly monotonicity of $g \mapsto \min(g,f)$ on that lattice.

LineSearch is already a Hilbert-space algorithm. Because it uses only the gradient's direction, nothing in it needs finite dimension — except the finite-difference sampling along $N+1$ axes. Replacing that with $m \ll N$ Gaussian probes is the one required change, and I flagged the caveat optimism usually drops: derivative-free rates are not dimension-free (Nesterov–Spokoiny), so it only works given a low-dimensional active subspace, which weighted domains supply.

Two practical notes. The first thing to break is the tree, not the theory — a $2^N$ fan-out is 1024 children at $N{=}10$ and a million at $N{=}20$; weight-ordered $k$-d splits fix that without touching a single proof, moving the ceiling from roughly $N{=}10$ to $N{=}10^2$. And there's a zero-code experiment: plot $\log d_k$ against $\log k$ on your existing runs. Greedy covering radius scales as $k^{-1/N}$, so the slope measures effective dimension — a non-integer slope on the fractal distribution would be a genuinely good readme figure.

The proposals in §6 are speculative and labelled so; the negative results in §2 and §7 are derived. Nothing is committed.