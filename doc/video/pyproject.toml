[project]
name = "manimations"
version = "0.1.0"
description = "Manim animations for the space-filling explainer video"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "manim>=0.20.1",
]
