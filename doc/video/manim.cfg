# Project-wide manim defaults. The final composition is QHD (2560x1440);
# render it with `-qp`. Quality flags (-ql/-qm/-qh/-qp) override the
# resolution/frame_rate set here (e.g. -ql for fast previews).
[CLI]
frame_rate = 60
pixel_width = 2560
pixel_height = 1440
